{
  "name": "@virtualcoach/monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "RAG Knowledge Base SAAS Platform - Monorepo",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "type-check": "npm run type-check --workspaces --if-present",
    "clean": "rm -rf node_modules packages/*/node_modules services/*/node_modules backend/node_modules frontend/node_modules",
    "dev": "npm run dev --workspace=backend & npm run dev --workspace=frontend"
  },
  "devDependencies": {
    "jest-util": "^29.7.0",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
