# =============================================================================
# RAG KNOWLEDGE BASE SAAS PLATFORM - Environment Configuration
# Version 1.0.0
# =============================================================================

# =============================================================================
# DATABASE
# =============================================================================
DATABASE_URL=postgresql://digitaltwin_user:digitaltwin_pass@localhost:5432/digitaltwin

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================
PORT=3001
NODE_ENV=development
CORS_ORIGIN=http://localhost:3000
LOG_LEVEL=debug

# =============================================================================
# AUTHENTICATION & SECURITY
# =============================================================================
# JWT Secret - MUST be changed in production (min 32 characters recommended)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Encryption Key - MUST be changed in production (min 32 characters required)
# Used for encrypting OAuth tokens and sensitive data
ENCRYPTION_KEY=your-encryption-key-min-32-characters-change-in-production

# =============================================================================
# LLM PROVIDERS (at least one required)
# =============================================================================
# OpenAI (GPT-3.5, GPT-4, embeddings)
OPENAI_API_KEY=sk-your-openai-api-key

# Anthropic (Claude 3 Opus, Sonnet, Haiku, Claude 3.5 Sonnet)
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key

# Google Gemini (Gemini Pro, Gemini Pro Vision)
GEMINI_API_KEY=your-gemini-api-key

# =============================================================================
# FRONTEND
# =============================================================================
FRONTEND_URL=http://localhost:3000
REACT_APP_URL=http://localhost:3000
REACT_APP_API_URL=http://localhost:3001
REACT_APP_WS_URL=ws://localhost:3001

# =============================================================================
# OAUTH AUTHENTICATION - USER LOGIN
# =============================================================================
# Google OAuth (for user login)
GOOGLE_OAUTH_CLIENT_ID=your-google-oauth-client-id
GOOGLE_OAUTH_CLIENT_SECRET=your-google-oauth-client-secret
GOOGLE_OAUTH_REDIRECT_URI=http://localhost:3001/api/oauth/auth/google/callback

# GitHub OAuth (for user login)
GITHUB_OAUTH_CLIENT_ID=your-github-oauth-client-id
GITHUB_OAUTH_CLIENT_SECRET=your-github-oauth-client-secret
GITHUB_OAUTH_REDIRECT_URI=http://localhost:3001/api/oauth/auth/github/callback

# =============================================================================
# OAUTH AUTHENTICATION - EMAIL INTEGRATION
# =============================================================================
# Gmail OAuth (for email sync)
GMAIL_CLIENT_ID=your-gmail-oauth-client-id
GMAIL_CLIENT_SECRET=your-gmail-oauth-client-secret
GMAIL_REDIRECT_URI=http://localhost:3001/api/email/auth/gmail/callback

# Outlook OAuth (for email sync)
OUTLOOK_CLIENT_ID=your-outlook-oauth-client-id
OUTLOOK_CLIENT_SECRET=your-outlook-oauth-client-secret
OUTLOOK_REDIRECT_URI=http://localhost:3001/api/email/auth/outlook/callback

# =============================================================================
# RAG LOGGING (OPTIONAL)
# =============================================================================
# Enable verbose RAG pipeline logging (query enhancement, search, reranking)
RAG_LOG_VERBOSE=false

# =============================================================================
# WEB SCRAPING (OPTIONAL)
# =============================================================================
# Redis URL for web scraping job queue (BullMQ)
WEB_SCRAPING_REDIS_URL=redis://localhost:6379

# Enable screenshot capture during web scraping (for debugging)
SCRAPER_ENABLE_SCREENSHOTS=false

# =============================================================================
# PRODUCTION-ONLY SETTINGS
# =============================================================================
# Uncomment and configure these for production deployment

# Stripe Billing (if using subscription features)
# STRIPE_SECRET_KEY=sk_live_your-stripe-secret-key
# STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret

# Production SSL/HTTPS
# NODE_ENV=production
# CORS_ORIGIN=https://yourdomain.com
# FRONTEND_URL=https://yourdomain.com

# Production OAuth Callbacks (update after deployment)
# GOOGLE_OAUTH_REDIRECT_URI=https://yourdomain.com/api/oauth/auth/google/callback
# GITHUB_OAUTH_REDIRECT_URI=https://yourdomain.com/api/oauth/auth/github/callback
# GMAIL_REDIRECT_URI=https://yourdomain.com/api/email/auth/gmail/callback
# OUTLOOK_REDIRECT_URI=https://yourdomain.com/api/email/auth/outlook/callback
